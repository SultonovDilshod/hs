# Local data is user-specific and shouldn't be tracked in git.
# Each user generates their own data.generated.js from the CSVs they
# drop into dashboard/data/.
/dashboard/data/
/dashboard/data.generated.js
